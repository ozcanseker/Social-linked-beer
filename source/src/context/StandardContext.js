import React from "react";

/**
 * Context that spreads the solidCommunicator and modelHolder
 * @type {React.Context<{SUCK_ON: string}>}
 */
const StandardContext = React.createContext({SUCK_ON : "THEZE NUTZ"});

export default StandardContext;